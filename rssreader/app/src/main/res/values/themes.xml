<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this theme only decides what the window looks like in the
         moment between launch and the first frame. -->
    <style name="Theme.Khabar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
